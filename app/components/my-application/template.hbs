<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">

      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          <li>{{#link-to "parties"}}MY PARTIES{{/link-to}}</li>
          <li>{{#link-to "create-party"}}CREATE PARTY{{/link-to}}</li>
        {{/if}}
      </ul>

      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}CHANGE PASSWORD{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>SIGN OUT</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}SIGN UP{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}SIGN IN{{/link-to}}</li>

        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<h1>Pin Planner.</h1>

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="wrapper">
  {{outlet}}
</div>
